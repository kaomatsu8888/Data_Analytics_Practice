"""
# pythonに関する説明で、誤っているものはどれか。

選択 1

インタープリタはend()で終了する。# 正解quit()で終了

選択 2

インタープリタでコマンド入力を促すプロンプトを「プライマリプロンプト」という。

選択 3

デフォルトではPythonのソースコードはUTF-8でエンコードしてあるものとして扱われる。

選択 4

デフォルト以外のエンコーディングを使うことができる。
"""
"""
Pythonに関する説明で誤っているものを見つけるため、それぞれの選択肢を見ていきましょう。

選択 1
"インタープリタはend()で終了する。"

この選択肢は誤りです。Pythonインタープリタを終了する標準的な方法は quit() 関数を使うか、または特定の環境
（UNIX系システムでは Ctrl+D、Windowsでは Ctrl+Z と Enter）で EOF（End Of File）シグナルを送ることです。end() 関数はPythonインタープリタを終了するためのものではありません。

選択 2
"インタープリタでコマンド入力を促すプロンプトを「プライマリプロンプト」という。"

これは正しいです。Pythonのインタープリタで一般的に見られる >>> は、プライマリプロンプトと呼ばれ、コマンドの入力を促します。

選択 3
"デフォルトではPythonのソースコードはUTF-8でエンコードしてあるものとして扱われる。"

これも正しいです。Python 3からは、ソースコードはデフォルトで UTF-8 でエンコードされているものとして扱われます。これにより、多言語の文字を直接ソースコードに含めることが容易になりました。

選択 4
"デフォルト以外のエンコーディングを使うことができる。"

これも正しいです。ファイルの先頭にエンコーディングの宣言を行うことで、Pythonのソースコードはデフォルト以外のエンコーディングを使用して書くことができます。例えば、# -*- coding: cp1252 -*- のような宣言をファイルの先頭に追加することで、特定のエンコーディングを指定することが可能です。

**結論として、誤っている説明は選択 1「インタープリタはend()で終了する。」です。**正しい終了方法は quit() を使用することです。
"""
