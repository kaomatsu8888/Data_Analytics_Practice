"""
Pythonの文字列に関する説明で、正しいものはどれか。

選択 1

文字列は+演算子で連結できない。

選択 2

文字列はインデックス指定（番号による指定）ができない。

選択 3

Pythonの文字列は値を変更できる（ミュータブル）。

選択 4

文字列リテラルを複数行にわたって書く場合は、トリプルクオーテーション（""" か '''）で囲う。
ChatGPT
Pythonの文字列に関する特徴を見てみましょう。

選択 1: 文字列は+演算子で連結できない。
これは誤りです。Pythonでは、+ 演算子を使って文字列を連結することができます。例えば、"Hello, " + "world!" は "Hello, world!" という一つの文字列になります。

選択 2: 文字列はインデックス指定（番号による指定）ができない。
これも誤りです。Pythonの文字列はインデックス指定でアクセスすることができます。文字列内の特定の文字にアクセスするには、文字列[インデックス] の形式を使います。インデックスは0から始まります。

選択 3: Pythonの文字列は値を変更できる（ミュータブル）。
これは誤りです。Pythonの文字列は不変（イミュータブル）です。つまり、文字列が一度作成されると、その内容を変更することはできません。新しい文字列を作成する必要があります。

選択 4: 文字列リテラルを複数行にわたって書く場合は、トリプルクオーテーション（""" か '''）で囲う。
これは正しいです。Pythonでは、トリプルクオート（""" または '''）を使用することで、複数行にわたる文字列リテラルを作成することができます。これにより、改行を含む文字列を簡単に定義できます。

正しい選択は選択 4「文字列リテラルを複数行にわたって書く場合は、トリプルクオーテーション（""" か '''）で囲う。」です。

"""
"""
